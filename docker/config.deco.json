{ 
  "listenAddress": ":8080",
  "account": {
    "region": "us-east-1",
    "akid": "{{ .akid }}",
    "secret": "{{ .secret }}",
    "externalId": "{{ .external_id }}",
    "role": "{{ .cross_account_role }}"
  },
  "accountsMap": {
    "spinup": "{{ .spinup_acount_id }}",
    "spinupsec": "{{ .spinupsec_acount_id }}"
  },
  "proxyBackend": {
    "baseUrl": "{{ .proxy_base_url }}",
    "backendPrefix": "{{ .proxy_backend_prefix }}",
    "token": "{{ .proxy_token }}"
  },
  "token": "{{ .api_token }}",
  "logLevel": "{{ .log_level }}",
  "org": "{{ .spinup_org }}"
}
